class SCC(object):
    def __init__(self):
        pass

    def depth_first_search(self, data, order_of_node=[]):
        if order_of_node:
            depth_first_search_rec        

    def depth_first_search_rec(self, data, node, discovered_nodes, order_of_node=[]):
        for connection in data:
            if connection in discovered_nodes:
                continue
            s.add(connection)
